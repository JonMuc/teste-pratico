import 'package:flutter/material.dart';

//Definicao de todos os estilos do APP
class ThemeApp {
  static const Color subText = Color.fromRGBO(112, 112, 112, 1);
  static const Color appBarHeader = Colors.black87;

  //Bcakground gradient home
  static const Color corBase = Colors.white;
  static const Color corMax = Colors.grey;

}
